
in this assignment we had to make a simple program to test if all our 
programming tools are installed and working properly.

the program creates a string called name which holds my name
the program creates a char* called charName which holds a  my name

the function message takes in a string and couts the string to the console

the program calls the message function twice.
Once with name.and once with charName

